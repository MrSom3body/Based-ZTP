services:
  checkmk:
    image: "checkmk/check-mk-raw:2.4.0-latest"
    cap_add:
      - NET_RAW
    environment:
      - CMK_PASSWORD=Juniorcisco123!
      - TZ=Europe/Vienna
    volumes:
      - monitoring:/omd/sites
    ports:
      - "8080:5000"
      - "8000:8000"
    restart: always

volumes:
  monitoring:
